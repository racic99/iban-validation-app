import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 32,
  },
  text: {
    fontSize: 32,
    marginTop: 16,
  },
});
